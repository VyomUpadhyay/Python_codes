Principal = float(input("Enter the Principal Amount => "))
RateOfInterest = float(input("Enter the Rate Of Interest (Per Anum) => "))
TimePeriod = float(input("Enter the Time Period => "))

SimpleInterest = Principal * RateOfInterest * TimePeriod / 100

print("Simple Interest of the following data = ", SimpleInterest)